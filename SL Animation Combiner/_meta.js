/* SL Tools — existence marker. Used by ../_shared/sl-nav.js to detect
   that this tool's folder is present. Removing this file (or the
   whole folder) hides the tool from every other tool's nav bar. */
window.__SLT_AVAILABLE = window.__SLT_AVAILABLE || {};
window.__SLT_AVAILABLE['anim-combiner'] = { present: true };
